[workspace]
members = [
    "command_line_tool",
    "pixelmap",
    "viewer",
]

resolver = "2"

[profile.release]
# codegen-units = 1 so LTO can actually inline across the whole hot path.
lto = "fat"
codegen-units = 1
