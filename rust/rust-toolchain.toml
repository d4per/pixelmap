# Pinning the channel means everyone compiles the workspace with the same rustc, and the
# crates around the library are not held back to its MSRV. The library's own
# minimum (1.80) is checked separately by CI, not here.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
